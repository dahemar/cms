# Database
DATABASE_URL="postgresql://user:password@localhost:5432/mydb?schema=public"

# Session Secret (generate a random string)
SESSION_SECRET="your-session-secret-here"

# Server Port (optional, defaults to 3000, Vercel uses PORT env var)
PORT=3000

# Email Configuration (for password reset and verification)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_FROM=your-email@gmail.com

# Google OAuth (optional)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3000/auth/google/callback

# API URL (for CORS and frontend)
API_URL=http://localhost:3000

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# ============================================================
# GitHub Integration: Auto-rebuild frontends when content is published
# ============================================================

# Method 1: GitHub App (RECOMMENDED for production)
# - Tokens rotate automatically (~1 hour TTL)
# - Scalable to multiple repos without individual PATs
# - See: GITHUB_APP_SETUP.md for full setup guide
# Replace these values with real ones for local development
GITHUB_APP_ID=2681636
GITHUB_APP_PRIVATE_KEY_PATH=/Users/david/Documents/GitHub/cms/backend/cms-app-dhm.2026-01-18.private-key.pem
# Or inline (use \n for newlines):
# GITHUB_APP_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\nMIIE...\n-----END RSA PRIVATE KEY-----\n"
GITHUB_APP_INSTALLATION_ID=12345678
GITHUB_FRONTEND_REPOS=dahemar/sympaathy-v2,dahemar/cineclub

# GitHub App Webhook Secret (for signature verification)
# Generate with: openssl rand -hex 32
GITHUB_WEBHOOK_SECRET=9f3a1c7e5d2f08a4c6b7e9d1f2a3c4e5b6d7f809a1b2c3d4e5f6a7b8c9d0e1f2

# Method 2: PAT (fallback, simpler but less secure)
# - Use for quick setup or single repo
# - Requires manual rotation (90 days expiration)
# - See: GITHUB_ACTIONS_REBUILD_SETUP.md
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
GITHUB_REPO_OWNER=dahemar
GITHUB_REPO_NAME=sympaathy-v2
